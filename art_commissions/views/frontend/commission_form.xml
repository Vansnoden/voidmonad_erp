<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="commission_request_form" name="Commission Request Form">
        <t t-call="website.layout">
            <div class="row p-0">
                <div class="d-flex flex-column justify-content-center align-items-center 
                    col-md-4 col-sm-6 col-xs-12 bg-primary p-4 text-justify" style="min-height:60vh">
                    <h1 class="text-center mb-4">Commission request Submission</h1>
                    <p class="mt-2 p-4">
                        Welcome to the commission submission page. <br/>
                        Here you can submit your project requests for review and processing. <br/>
                        Before proceeding, please take a moment to consult the submission policy 
                        (<a href="/commission-policy" target="_blank"> <i class="fa fa-link"></i> here </a>.), 
                        which outlines the requirements, guidelines, and conditions for acceptance. <br/>
                        This will help ensure that your request is complete and can be handled smoothly.
                    </p>
                </div>
                <div class="col-md-8 col-sm-6 col-xs-12 border-left">
                    <form action="/commission/submit" method="post" class="p-4" enctype="multipart/form-data">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div class="form-group">
                            <label class="mr-2">What type of commission are you looking for ?</label>
                            <label for="fanart" class="ml-3" > 
                                Fan Art <input type="radio" id="fanart" name="category" value="fanart" required="true"/> 
                            </label>
                            <label for="original" class="ml-3" > 
                                Original Art <input type="radio" id="original" name="category" value="original"/> 
                            </label>
                        </div>
                        <div class="form-group">
                            <label class="mr-2">Describe your request bellow:</label>
                            <textarea rows="5" cols="50" class="form-control" name="description" required="true"></textarea>
                        </div>
                        <div class="form-group">
                            <label class="mr-2">
                                Do you have any references picture that could help
                                in realizing your vision ? (upload them bellow)
                            </label>
                            <input type="file" 
                                class="form-control" name="images[]" 
                                accept="image/png, image/jpeg" multiple="True"/>
                        </div>
                        <div class="d-flex flex-row justify-content-end align-items-center mt-3 border-top py-2">
                            <button type="submit" class="btn btn-success mr-3">Submit</button>
                            <button type="cancel" class="btn btn-warning">Cancel</button>
                        </div>
                        <div t-if="success" class="mt-4 alert alert-success" role="alert">
                            Thank you for your submission, your request has been received successfully.
                        </div>
                        <div t-if="error" class="mt-4 alert alert-danger" role="alert">
                            Sorry, an error occurred: 
                            <p>
                                <t t-esc="error"/>
                            </p>
                        </div>
                    </form>
                </div>
            </div>
        </t>
    </template>

</odoo>
